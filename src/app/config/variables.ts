const config = {
  apiBaseUrl: "http://localhost:8080", // URL base para requisições no backend
  frontBaseUrl: "http://localhost:3000", // URL base para acesso ao frontend
};

export default config;
